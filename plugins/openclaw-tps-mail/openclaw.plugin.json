{
  "id": "openclaw-tps-mail",
  "channels": [
    "tps-mail"
  ],
  "activation": {
    "onStartup": true
  },
  "channelConfigs": {
    "tps-mail": {
      "schema": {
        "type": "object",
        "properties": {
          "enabled": { "type": "boolean" },
          "accounts": { "type": "object" }
        }
      },
      "uiHints": {
        "mailDir": {
          "label": "TPS Mail Directory",
          "placeholder": "~/.tps/mail",
          "help": "Root directory containing per-agent mail inboxes (new/, cur/, tmp/, dlq/, outbox/)"
        }
      },
      "label": "TPS Mail",
      "description": "Inter-agent messaging via the local filesystem mail queue"
    }
  },
  "uiHints": {
    "mailDir": {
      "label": "TPS Mail Directory",
      "placeholder": "~/.tps/mail",
      "help": "Root directory containing per-agent mail inboxes (new/, cur/, tmp/, dlq/, outbox/)",
      "advanced": true
    }
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "mailDir": {
        "type": "string"
      },
      "obligationRetentionDays": {
        "type": "number",
        "description": "Days a TERMINAL obligation record (acked/failed) is kept after its last transition before startup recovery deletes it. Default 7; <= 0 disables the sweep."
      }
    }
  }
}

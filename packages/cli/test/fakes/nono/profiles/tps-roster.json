{
  "$schema": "https://nono.sh/schemas/nono-profile.schema.json",
  "extends": "tps-base",
  "meta": {
    "name": "tps-roster",
    "version": "2.0.0",
    "description": "tps roster — read-only org chart. Reads ~/.openclaw/openclaw.json as metadata (the base profile denies it to agents; this operator-side profile exempts it explicitly) and nothing else. No writes, no network."
  },
  "groups": {
    "exclude": ["system_write_macos", "system_write_linux", "user_tools"]
  },
  "workdir": {
    "access": "read"
  },
  "filesystem": {
    "bypass_protection": ["~/.openclaw/openclaw.json"],
    "read_file": ["~/.openclaw/openclaw.json"]
  },
  "network": {
    "block": true
  }
}
